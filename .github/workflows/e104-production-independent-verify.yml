name: E104 independent production verifier

on:
  push:
    branches:
      - research/e104-haar-radial-raoblackwell-20260918
    paths:
      - 'research/E104_PRODUCTION_VERIFY_FREEZE.json'
      - 'scripts/e104_production_independent_verify.py'
      - '.github/workflows/e104-production-independent-verify.yml'

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      PYTHONHASHSEED: '0'
      OPENBLAS_NUM_THREADS: '1'
      OMP_NUM_THREADS: '1'
      MKL_NUM_THREADS: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install frozen project dependencies
        run: python -m pip install -e .
      - name: Run independent E104 production verifier
        run: python scripts/e104_production_independent_verify.py
      - name: Upload immutable E104 production verification
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e104-production-independent-verify
          path: e104-production-independent-verify.json
          if-no-files-found: error
          retention-days: 90
