# E119 terminal closure and E121+ successor gate — 2026-09-19

Control key: `ARC-CONTROL-E119-TERMINAL-E121PLUS-GATE-20260919`

Supersedes control head: `16e3b6c6fad499c1b825c678845fccff6e5c6200`.

## Terminal closure

E119 is **terminally closed**.

Authoritative owner lane remains:

`research/e119-generic-boundary-flux-certificate-20260919`

Sole authoritative owner scientific execution remains:

- run `35458020001`
- job `105936659806`
- artifact `10589345345`
- artifact SHA256 `7485a4380bca0175197afeb327aed9abeb2f5d14ca335cdff6571ad0c7ed78e3`

No E119 scientific rerun, rescue, retune, rank/cell/norm/seed change, new E119 mechanism, workflow dispatch, or scope promotion is authorized. Any such event is a control violation and must be recorded, not normalized into evidence.

Frozen E119 classification:

- small-width generic construction/certificate: verified only for input dimension 2, width<=8, depth<=4;
- material nonzero-atom compression: not established;
- direct full-dimensional production materialization: terminal NO-GO;
- production scientific GO: false;
- competition GO: false;
- public/public-mini/scorer/holdout/full: blocked.

## Successor namespace gate

Successor science must use a **fresh collision-free experiment ID E121 or later**.

At this control commit, repository branch search and code search found no E121, E122, E123, or E124 branch/content collision. This is a point-in-time namespace observation only; the monitor must re-check live repository state before accepting any future candidate ID.

An ID is collision-free only if, immediately before its protocol-first commit is accepted, there is no pre-existing scientific mechanism, branch, durable receipt, run, artifact, issue/PR identity, or conflicting control allocation using that experiment ID. If collision is detected, the candidate must move to the next unused E-ID before implementation.

## Protocol-first hard gate

For the first fresh E121+ candidate, the first scientific commit under that experiment identity must be **protocol-only**. It must precede candidate implementation, scientific workflow execution, target/reference inspection used for selection, and result receipt.

The protocol must freeze at minimum:

1. candidate mechanism and production-shape path;
2. target/oracle firewall;
3. target-free remainder/error certificate and deterministic composition rule;
4. production all-in FLOP accounting classes and cap;
5. pass/fail gates;
6. one-run/rerun policy;
7. allowed verification-only reference access;
8. prohibited tuning/sweep/rescue operations.

No implementation or scientific run is authorized until the protocol-first audit passes.

## First fresh candidate receipt audit

When the first fresh E121+ candidate receipt appears, the monitor must inspect live commits, branch ancestry, workflow runs/jobs/artifacts, source and receipt hashes. It must audit three gates before any GO can be recognized.

### 1. Target/oracle leakage

Required PASS conditions:

- candidate construction, selection, preprocessing, normalization, thresholds, hyperparameters, stopping and mechanism choice are independent of benchmark/public/public-mini/scorer/holdout/full targets;
- exact/reference/oracle values are unavailable to candidate code until candidate output/state relevant to the frozen gate is committed/frozen;
- post-hoc reference comparison cannot feed back into candidate selection or rerun;
- target-free claim is supported by code/data-flow evidence, not only receipt text.

Any target-dependent fit/select/tune/normalize/stopping path is terminal for that candidate receipt unless the preregistered protocol explicitly permits a disjoint non-benchmark calibration corpus.

### 2. Remainder/error proof

Required PASS conditions:

- proof is computable before oracle comparison;
- every approximation/error component is bounded, including omitted approximation, numerical geometry, helper routines and final materialization;
- missing component certificates fail closed and are never treated as zero;
- deterministic composition of component bounds is frozen in the protocol;
- total certified error is `<= 1.374772708486752e-4` for the relevant production claim;
- empirical small-width agreement cannot substitute for the production remainder proof.

### 3. Production all-in FLOPs

Required PASS conditions:

- production shape is explicit;
- all candidate, helper, geometry/root/sort/trig/mask/state, materialization, certificate, bookkeeping and required verification-at-deploy costs are counted if they are on the production path;
- reserves must be derived or measured under the frozen accounting model; unexplained lump-sum reserves fail;
- storage/state explosion that forces extra computation must be represented;
- total incremental production all-in FLOPs must be `<= 136,758,472,261`;
- small-width FLOPs cannot be extrapolated into a production GO without a valid production derivation.

## GO discipline

The first fresh E121+ receipt is **not** a GO merely because it exists or because its workflow succeeds.

Before a positive control classification, all three audits above must PASS and the protocol-first provenance must be intact. Otherwise the monitor must write an append-only control finding with exact SHAs/run/job/artifact IDs and assign exactly one bounded correction.

No status-only control turn is sufficient when a substantive blocker or violation is found: the finding/correction must be recorded durably in the control branch.

## Assigned next correction

**Correction C-E121PLUS-001**

Do not execute or authorize any successor implementation/workflow until a collision-free E121+ **protocol-only first commit** exists and passes the four-part intake check:

1. namespace collision check;
2. protocol-first ancestry check;
3. target/oracle firewall completeness;
4. preregistered production remainder + all-in FLOP proof structure.

When the first fresh candidate receipt later appears, audit its actual implementation/run evidence against the frozen protocol and record the next correction in this control branch if any gate fails.

E119 remains closed regardless of successor outcome.
