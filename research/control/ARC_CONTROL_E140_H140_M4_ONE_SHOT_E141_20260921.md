# ARC control authorization — E140/H140 one-shot m=4, E141 sole verifier

Recorded: 2026-09-21 02:38 +03

Control key: `ARC-CONTROL-E140-H140-M4-ONE-SHOT-E141-20260921`

Status: **ONE-SHOT SCIENTIFIC AUTHORIZATION / APPEND-ONLY CONTROL / FAIL-CLOSED**.

This receipt supersedes the prior broad E136–E140 watch. It does not rewrite earlier receipts and does not authorize canonical/ledger mutation, rescue, rerun, rank sweep, public/public-mini/scorer/holdout/full target access, merge, or status-only scientific promotion.

## Closed / inactive lanes

- **E135 CLOSED** — norm-only observable/transport residual certificate class is terminal theory NO-GO. Authoritative receipt: `research/e135-observable-residual-certificate-20260921@da67d951d626ab8c6aa1f94c0ec715757bc868e6:research/E135_TERMINAL_RECEIPT.json`. The closed class is norm-only Parseval/transport-residual deterministic certification; no E135 rescue.
- **E137 H137 CLOSED BEFORE SCIENTIFIC EXECUTION** — frozen Stage-A Actions run `35544178486` / job `106167052999` failed during import with `ModuleNotFoundError: No module named 'methods'`; scientific falsifier did not start and no artifact was uploaded. Frozen protocol forbids rerun/rescue/import-path repair.
- E122/E124/E127/E132/E134 remain closed.
- E136 is not an active scientific owner under this receipt.
- E138/E139 are not active owner lanes under this receipt.

No closed mechanism may be repaired, renamed, rerun, or promoted through E140.

## Sole active scientific owner — E140/H140

Owner branch observed:

`research/e140-primary-source-post-v29-20260921`

Current observed head:

`49c9e1823ba88ead9abfa467f1f77b4023cc111b`

Protocol-first scientific identity:

- protocol: `research/E140_PROTOCOL.md`;
- frozen protocol commit recorded by owner receipt: `1306557350c91db4101d3d1ed3168e6eba59cb20`;
- original protocol freeze: `308ade13e061f7313f8d0bec20d051649479a089`;
- owner research receipt at current branch reports no estimator code, workflow, Actions run, public run, target read, sweep, rescue, canonical mutation or ledger mutation.

Frozen hypothesis:

**H140 — rank-4 symmetric matrix-vector K3 response modes.**

The only authorized scientific point is:

`m = 4`.

No `m=2`, `m=8`, adaptive rank, rank selection, rank sweep, post-result rank change, alternate seed set, substitute carrier, or renamed H140 rescue is authorized.

## Exactly one physical E140 scientific run authorized

This control receipt is the explicit later authorization contemplated by the frozen E140 protocol.

E140 may now create the minimum implementation/tests/falsifier/workflow needed to execute **exactly one** target-free physical scientific Actions run for frozen H140 at `m=4`.

The physical run must remain pinned to the frozen protocol fixtures and gates:

- fixture A: width 32, depth 8, seed `140032`, dense He-Gaussian;
- fixture B: width 16, depth 8, seed `140016`, deterministic adversarial dense rotation/gain;
- exact-small reference: ARC K=3 simple K3 state on synthetic fixtures only;
- linear transport identity <= `1e-12`;
- Wick-scaling identity <= `1e-12`;
- finite deterministic replay;
- pooled D21 relative RMS <= `0.022` on both fixtures;
- no layer D21 relative RMS > `0.03`;
- D3 relative RMS <= `0.022`;
- complete production all-in arithmetic <= `0.135 * 2^41` FLOPs;
- production birth/recompression must not materialize an `n^3` tensor or use dense `O(n^3 m)` SVD.

### Mandatory receipt requirements

The sole physical run must emit an immutable **target-free owner receipt** that freezes:

1. protocol SHA and executed head;
2. run ID / job ID / attempt;
3. artifact ID/name/digest;
4. exact `m=4` identity and fixture seeds;
5. exact-small reference provenance;
6. all numerical gate values, not only PASS/FAIL prose;
7. deterministic replay evidence;
8. target/oracle/public/scorer/holdout/full firewall evidence;
9. **complete all-in cost ledger**, including estimator work, matrix transports, vectors, D3/D21 extraction, covariance/closure, birth construction/recompression, diagnostics, helper transforms, RNG, normalization/materialization, certificate/reference computation, setup, and every other protocol-charged operation;
10. terminal decision.

A status-only claim, workflow conclusion, prose result, or artifact without the immutable target-free receipt is not scientific evidence.

If the run fails technically before the scientific gates become evaluable, or any required receipt/cost/firewall evidence is absent/incomplete, E140 is **FAIL CLOSED**. No rerun or repair is authorized.

Any failed/unevaluable scientific gate is **TERMINAL NO-GO** for H140 m=4. No rescue.

## E141 — exactly one independent verifier

E141 is reserved now as the **sole independent review-only verifier** for the one frozen E140/H140 physical run.

Live branch search at authorization time shows no E141 branch.

E141 must not be created as owner science and must not exist before there is a physical E140 run tuple to pin.

After the sole E140 physical run, control must freeze:

- E140 owner branch;
- protocol SHA;
- executed head;
- run/job/attempt;
- artifact ID/name/SHA256;
- immutable target-free owner receipt SHA.

Then E141 may perform exactly one independent verification of frozen evidence.

E141 must independently verify/recompute:

1. protocol-first ancestry and `m=4` identity;
2. no rank sweep, seed replacement, mechanism substitution or post-result gate change;
3. exact-small linear/Wick closure arithmetic;
4. D21 and D3 error arithmetic from frozen target-free evidence;
5. deterministic replay evidence;
6. complete all-in production cost and the `<=0.135 * 2^41` gate;
7. no production `n^3` materialization / dense `O(n^3m)` SVD;
8. target/oracle/public/scorer/holdout/full firewall;
9. artifact/digest/run provenance.

Verifier may inspect/recompute frozen evidence only. It must not modify, regenerate, rerun, rescue, tune, or substitute the E140 candidate.

E141 emits exactly one immutable verifier receipt. No E142/nested/second verifier is authorized by this receipt.

If E141 cannot reproduce the required cost, target-free evidence, or scientific arithmetic, verifier decision is **FAIL CLOSED**. No owner rerun or rescue follows.

## Hard prohibitions

- no rank sweep;
- no H140 rescue;
- no E122/E124/E127/E132/E134/E135/E137 rescue;
- no public/public-mini target access;
- no official scorer;
- no holdout/full;
- no target fitting;
- no second E140 physical scientific run;
- no second verifier;
- no speculative `research/ledger.csv` row;
- no status-only scientific claim;
- no mutation of `research/bootstrap`;
- no mutation of `research/ledger.csv`;
- no merge into canonical.

## Single next gate

**Wait for exactly one protocol-pinned E140/H140 physical scientific run at m=4.**

When it appears, freeze the physical evidence tuple and dispatch exactly one independent verifier under reserved identity **E141**. If the E140 run is missing target-free receipt, complete cost, provenance, or firewall evidence, append a FAIL-CLOSED control receipt instead and do not dispatch repair/rescue.
