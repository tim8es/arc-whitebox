# E122 protocol — multi-source subspace state (protocol only)

Idempotency key: `ARC-E122-MULTISOURCE-SUBSPACE-STATE-20260919`.

Status: **PROTOCOL ONLY / NO SCIENTIFIC RUN AUTHORIZED BY E128**.

Parent lesson: E121 terminal NO-GO showed that a single shared 2-D cyclic source orbit is marginally correct and excellent in 2-D but loses high-dimensional source coverage: exact 8-D block-stress pooled candidate/iid MSE ratio was 11.435172907952149 against a frozen <=0.90 gate.

## Non-overlapping hypothesis

Preserve multiple independent source coordinates jointly through late ReLU layers using a fixed higher-dimensional source state (`k>=4`) plus target-free algebraic compression between layers. The mechanism must carry simultaneous multi-source dependence; it may not be implemented as a rebalance, randomization, mixture, or tuning of E121 two-plane phase orbits.

## Closed E121 fingerprint

E122 MUST NOT use as its scientific mechanism: Haar two-plane cyclic orbit stratification; P/M rebalancing; alternate E121 frame count; alternate E121 phase count; seed changes; phase randomization; fitted correction to E121; weighted E121 orbit nodes; or a second E121 run under a new name.

## Before code

A dedicated E122 owner must freeze an implementation-level protocol specifying: exact source-state representation and dimension; compression operator; synthetic exact-reference corpus; same-cost comparator; deterministic seeds; complete all-in FLOP accounting; numerical PASS/NO-GO gates; target-access audit; and one-run/no-rescue discipline.

The frozen falsifier must include a multi-source exact fixture at least as demanding as E121's 8-D four-block construction and must demonstrate improvement against a same-cost iid or otherwise preregistered comparator without reading exact references during candidate construction.

## Forbidden scope

No benchmark targets, public/public-mini, official scorer, holdout, full suite, production execution, target fitting, post-result tuning, rescue, or parameter sweep is authorized by this protocol-only allocation.

E128 allocation of E122 is an ID/hypothesis reservation, not a scientific claim.
