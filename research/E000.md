# E000 — Official Phase-2 covariance baseline reproduction

## Status

**PASS / complete.** E001 was not started.

## Provenance

- Official source repo: `AIcrowd/whest-starterkit`
- Pinned starter-kit commit: `3e1e3b5f3da3e03a128ff292d0b14251727aeb5e`
- Estimator: `examples/03_covariance_propagation.py`
- Downloaded file SHA256: `f5e7d97fc6093330c9bef7daaf0f9e54d84f58edf8e28bdd980eae4dac2e796d`
- Runtime: `whestbench==0.16.1`, `flopscope==0.12.1`, Python 3.11.16, NumPy 2.4.6
- Dataset: `hf://aicrowd/arc-whestbench-public-2026@v2-phase2`
- Split: `mini` (100 MLPs, width 1024, depth 16, baked N=1e9 ground truth)
- Runner: `local`
- Per-MLP FLOP budget: `2**41 = 2,199,023,255,552`

## Exact commands

Fetch the pinned official estimator:

```bash
curl -fsSL \
  https://raw.githubusercontent.com/AIcrowd/whest-starterkit/3e1e3b5f3da3e03a128ff292d0b14251727aeb5e/examples/03_covariance_propagation.py \
  -o /tmp/03_covariance_propagation.py
sha256sum /tmp/03_covariance_propagation.py
```

Validate it:

```bash
whest validate --estimator /tmp/03_covariance_propagation.py
```

Run the measured Phase-2 baseline:

```bash
whest run \
  --estimator /tmp/03_covariance_propagation.py \
  --dataset hf://aicrowd/arc-whestbench-public-2026@v2-phase2 \
  --split mini \
  --runner local
```

Repository verification:

```bash
ruff check .
pytest -q
whest validate --estimator estimator.py
```

## Measured metrics

| Metric | Measured |
|---|---:|
| `adjusted_final_layer_score` | `4.05e-07` |
| `final_layer_mse` | `4.05e-06` |
| `all_layers_mse` | `2.23e-06` |
| `mean_score_multiplier` | `0.10000000` |
| `mean_compute_utilization` | `0.02351464` |
| Estimator FLOPs, total / 100 MLPs | `5.17e+12` |
| Approx. mean estimator FLOPs / MLP | `5.170924e+10` |
| Failed MLPs | `0 / 100` |
| WhestBench reported scoring duration | `93.385850 s` |
| Full command wall time (dataset resolve/load + scoring) | `11:58.69` |

The measured MSEs match the official documented Phase-2 covariance reference (`4.050e-06` final-layer MSE, `2.229e-06` all-layers MSE) to the displayed precision, satisfying the E000 <=2% reproduction criterion.

## Evidence

- GitHub Actions run: `34689957585`
- Job: `103543358075`
- Artifact: `e000-baseline-log`, artifact ID `10296813170`
- Baseline run exit status: `0`
- Official estimator `whest validate`: PASS
- Repo `ruff check .`: PASS
- Repo `whest validate --estimator estimator.py`: PASS

## Cost

External paid compute/API cost: **$0**. The run used GitHub Actions and the public Hugging Face dataset, within the E000 `$0–2` budget.
