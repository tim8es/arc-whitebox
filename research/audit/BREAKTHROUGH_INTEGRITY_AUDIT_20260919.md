# ARC breakthrough integrity audit — novelty, leakage, cost, GO scope

Receipt key: `ARC-BREAKTHROUGH-INTEGRITY-AUDIT-20260919`

Branch: `review/breakthrough-integrity-audit-20260919`

Base control receipt: `control/arc-deep-error-frontier-20260919@4da4dca60bab429b2181fbe8f06d38a239e7e9b8`

Mode: repository evidence audit plus next-gate assignment. No scientific rerun, public/scorer/holdout/full execution, canonical mutation, or ledger mutation is performed by this audit.

## Evidence policy

Only repository-visible branch/commit history, GitHub Actions run/job state, job logs, and immutable artifact metadata count as evidence. Chat claims, branch names, workflow success by itself, and unexecuted protocols do not count as scientific GO.

This audit covers the current promoted/breakthrough lineage E100-E113 and all live same-ID branches observed for E109-E113.

## Executive verdict

There is **no current competition breakthrough GO**.

The strongest factual statements are:

1. E100 had a real local variance-reduction result, but its original QR cost accounting was materially low and was later corrected; `scientific_go=false`.
2. E104 Haar radial Rao-Blackwell had a real local scientific mechanism and production-shaped target-free verification, but its authorized one-shot public mini0 run was a terminal accuracy failure. E104 is therefore **competition NO-GO**.
3. E105-E108 are terminal negative results.
4. post-E108 deep Jensen has a valid target-free **diagnostic component GO**, not an estimator GO.
5. E109/E110 live sampling-coupling protocols are unexecuted and their own 0.35 variance-ratio breakthrough threshold is far too weak to imply the competition target.
6. E111 is terminal scientific NO-GO.
7. Both executed E112 mechanisms are terminal NO-GO; a third E112 Edgeworth protocol is an unexecuted same-ID collision.
8. E113 top-2 and top-4 are same-ID variants of one gate-conditioning family. Running both would be a parameter/mechanism sweep unless identity is resolved before execution.

## Identity / novelty firewall

### E100
Authoritative orthogonal Gaussian antithetic lane:
`research/e100-orthogonal-gaussian-antithetic-20260918@49ed8dc17de34463deea254da7d113f7fc828b97`.

Same-ID shrinkage collision is not part of this identity.

Novelty: real relative to earlier iid/V29 sampling because it changes the joint angular coupling while preserving Gaussian marginals.

Current classification: local Stage-A / law-review evidence only; **not scientific or competition GO**.

### E103
`research/e103-orthogonal-antithetic-production-shape-20260918@051e4f391fe86bac93949bb67788160ccbc26ce0`.

This is implementation/accounting evidence for the E100 sampling law, not a new scientific mechanism.

### E104
Authoritative scientific identity:
`research/e104-haar-radial-raoblackwell-20260918`.

The collision branch
`research/e104-orthogonal-antithetic-complete-billing-20260918`
must not be mixed into the Haar/Rao-Blackwell scientific identity.

Novelty: valid conditional-expectation/Rao-Blackwell step over Gaussian radius.

Current competition status is superseded by the benchmark-owner receipt:
`research/e104-benchmark-owner-successor-20260919@a2e1f0eef872ad65a1887e05fae6b59bc8686b55`.

### E105-E108
E105 two-Haar risk certificate: diagnostic, terminal NO-GO.

E106 quartic Haar CV: materially new first-layer quartic control family; terminal NO-GO, independently verified.

E107 exact spherical fourth-moment cubature: materially distinct deterministic cubature; terminal analytic/pre-code NO-GO on cost.

E108 exact first-layer mean transported CV: materially distinct exact-mean transport control; terminal NO-GO.

### post-E108 Jensen
`research/post-e108-deep-final-jensen-variance-20260919@af5ede75b92ec417208044a6b629f4c3700ea0be`.

Novelty: valid exact deep nonlinear error decomposition.

But it does **not** change the estimator. Its measured deep-Jensen share is diagnostic:
`deep_jensen_over_total = 0.12124248666799274`.

Therefore `TARGET_FREE_DEEP_NONLINEAR_VARIANCE_GO` means only “a material error component exists.” It must never be cited as candidate/scientific/competition GO.

### E109 collision set
Observed distinct same-ID mechanisms include:

- coupled Haar + Walsh-Hadamard angular design;
- later-layer nonlinear centering structural gate;
- deep Stein/JVP CV;
- late gate-pair sign CV;
- spherical Stein/Jacobian CV.

Bare “E109 result” is invalid evidence. Every citation must include branch + idempotency key.

The coupled Haar-Hadamard branch remains protocol-only with zero Actions runs.

The later-layer nonlinear-centering falsifier physically ran:
run `35453144708`, job `105923684784`, artifact `10587143381`,
SHA256 `bf38c7c8fb2efcbb044d1925dcdcc1693f424c351b877087fb11fadc741bd76d`,
and established structural NO-GO for second-moment + antisymmetric centering.

Deep Stein/JVP and late gate-pair sign variants have terminal NO-GO receipts. No E109 variant has current GO evidence.

### E110 collision set
Observed distinct same-ID mechanisms include:

- deep-sensitivity Householder orbit;
- deep Jacobian fourth harmonic;
- deep line Rao-Blackwell.

Bare “E110 result” is invalid.

Householder owner branch:
`research/e110-deep-householder-orbit-20260919@ca6d9430644e745d174c46cbc27334332671d1f0`
is **protocol-only** and has zero Actions runs.

Independent exact verifier branch:
`review/e110-householder-smallwidth-exact-verifier-20260919@5e8e2846e71569374b85a8ed860d156c82ae882b`
is also protocol-only and has zero Actions runs.

Important process mismatch: the owner E110 protocol requires a committed pre-code derivation/orthogonality/accounting gate before implementation, while the independent verifier protocol was frozen before that owner evidence exists. Therefore the verifier must not be treated as confirmation of an admitted owner candidate until that gap is resolved.

The other E110 mechanisms are blocked/terminal negative lanes.

### E111
`research/e111-late4-gaussian-relu-plugin-cost-20260919@9d0dcc429fc4b62a6607ea28c97fbb44327dbaf2`.

Exact small-width evidence shows terminal scientific NO-GO:
final mean-bias MSE `0.0016698051697168073`, about `88349x` the raw target.

This is useful negative evidence: later-layer non-Gaussian shape cannot be reduced to exact mean/variance plus Gaussian ReLU closure.

### E112 collision set
At least three distinct same-ID mechanisms exist:

- exact activation-mask junction-tree/message passing;
- factorized fourth-cumulant joint-CGF / Edgeworth closure;
- separate Edgeworth-4 scalar plug-in protocol.

These may not be merged or cherry-picked as one E112 experiment.

Exact mask-message physical evidence:
- run `35455011080`
- job `105928633045`
- artifact `10588420977`
- artifact SHA256 `f6f1173527c3d659e9a3faea78e42b531d94c5423ec4b5ebb539b3f87e8bcfff`
- decision `TERMINAL_STRUCTURAL_NO_GO_DROP`
- reason: dense factors imply treewidth 1023 and a 2^1024 explicit state table.

Factorized cumulant physical evidence:
- run `35455150908`
- job `105929003614`
- artifact `10588441092`
- artifact SHA256 `61cdcf57a364f1a30b38e290dc188d19ddd7fdcd1b179214e1723aa9e2d5be3d`
- workflow conclusion `success`
- scientific decision `TERMINAL_NO_GO_DROP`
- final bias MSE `0.004138954933770478`
- final bias / E111 Gaussian bias `2.4787053057647612`
- negative predicted ReLU mean/variance states occur
- no targets read.

Workflow success here means **execution succeeded**, not scientific GO.

The separate E112 Edgeworth-4 protocol is unexecuted and strongly overlaps the failed cumulant closure's scalar Edgeworth component. It is not admissible as an E112 rescue without a new experiment identity and an explicit semantic-difference proof.

### E113 collision set
Two protocol-only same-ID branches exist:

- top-2 gate conditional moments:
  `research/e113-top2-gate-conditional-moments-20260919@e51c92b159f6c2b99a1d1a0653b9cf5df7c17e4a`
- top-4 gate-conditioned plug-in:
  `research/e113-top4-gate-conditioned-plugin-20260919@f7a38cdbcbefc48af880a51f24aefb90fc5b9e0d`

Both use the same frozen exact fixture family and differ mainly in how many parent gate bits are retained. Executing both after seeing either result would be a hidden parent-count sweep.

Neither has an Actions run. Neither is GO.

For audit purposes, chronology freezes the earlier protocol-only branch, top-2, as the only admissible E113 identity. The top-4 branch is an ignored same-ID collision and must not execute as an E113 rescue.

## Target leakage audit

### Direct target access event

E104 benchmark-owner one-shot mini0 is a real direct public-target read:

- run `35449587126`
- job `105914307484`
- artifact `10585484845`
- artifact SHA256 `160f4d7f8479226cb00fbf13c47951444ca014cba9896bf5ccf76036e3dc6c25`
- public dataset: `aicrowd/arc-whestbench-public-2026`
- split/index: `mini / 0`
- target field: `final_means`
- public records read: `1`
- measured raw MSE: `9.730479418246265e-6`
- raw target: `1.89e-8`
- ratio: `514.8401808595908x`
- verdict: terminal E104 mini0 NO-GO.

The run was single-shot, no sweep, no official scorer, no holdout/full.

### Contamination boundary

This event creates a **portfolio-level public-target contamination boundary**.

Later branches may truthfully say that their *workflow/script* reads no targets, but post-mini0 hypothesis selection occurred after a public target result was visible in the repository. Therefore post-boundary work must not claim that the overall research portfolio remained target-naive.

No later gate may use the mini0 MSE, residuals, coordinate errors, or target tensor to choose:
- mechanism;
- gate count;
- coefficient;
- seed;
- rank;
- threshold;
- parent count;
- stopping rule.

The only allowed use is historical classification: E104 is closed.

## Cost-accounting audit

### E100 — material undercount, corrected
Independent review commit:
`1fd08b93c3c2f5a05bbf2d9058a560c1a8e1cc65`.

The original QR line was low by roughly 4x because float64 full Q/R billing was understated.
Corrected planning envelope: `151,000,000,000` FLOPs,
utilization `0.06866684998385608`.
Cost feasibility survives, but the original static number is invalid.

### E103 — RNG undercount, corrected
Commit:
`051e4f391fe86bac93949bb67788160ccbc26ce0`.

Plain NumPy RNG was outside flopscope.
Minimum missing RNG bill: `67,174,400` FLOPs.
Corrected minimum total: `149,114,620,592`,
utilization `0.06780947869265219`.
Complete measured package-safe billing was not established under E103.

### E108 — pre-code upper was not conservative
Measured complete billing:
`187,819,635,376` FLOPs,
versus pre-code `184,474,289,328`.

Undercount:
`3,345,346,048` FLOPs, approximately `1.8134%`.

The lane still passes the utilization cap, so this is evidence-hygiene failure, not the reason for scientific NO-GO.

### E112 cumulant — receipt field inconsistency
Frozen protocol states:
`11 * 16 * 1024^2 = 184,549,376` dense-core FLOPs and total upper
`5,184,549,376`.

The executed JSON reports `dense_core_flops=46,137,344` while retaining
`production_upper_flops=5,184,549,376`.

The latter remains conservative with enormous slack, so this does not change the terminal scientific verdict, but the internal cost fields are inconsistent and must not be described as an exact production ledger.

### Current live protocols
E109 coupled-Hadamard, E110 Householder and E113 gate-conditioned branches have no measured production accounting. Protocol arithmetic is not measured evidence. They remain pre-execution.

## Fake-GO audit

The following classifications are prohibited:

- E100 local Stage-A GO -> competition GO: **invalid**
- historical E104 local scientific GO -> current competition GO: **invalid; superseded by mini0 terminal NO-GO**
- post-E108 deep-Jensen component GO -> estimator GO: **invalid**
- GitHub workflow conclusion success -> scientific GO: **invalid**
- E112 falsifier execution success -> E112 GO: **invalid; both executed E112 mechanisms are terminal NO-GO**
- E109/E110 preregistered “breakthrough gate” before a run -> GO: **invalid**
- E113 protocol existence -> GO: **invalid**

Additionally, the E109/E110 sampler-coupling threshold
`candidate/baseline risk <=0.35`
is only a mechanistic variance gate. Applied to the E105 measured risk
`8.604586912926751e-6`, even a 0.35 ratio gives approximately
`3.0116054e-6`, still about `159.34x` above the raw target.
Therefore such a pass cannot be labeled competition-path sufficient.

## Assigned next exact gate

### Gate ID
`ARC-BREAKTHROUGH-NEXT-GATE-E113-TOP2-EXACT-20260919`

### Why this lane
E111 and E112 show that the key unresolved blocker is later-layer non-Gaussian **dependence**, not merely marginal skew/kurtosis. Full exact mask inference is exponentially infeasible. A frozen low-cardinality gate-conditioned closure is therefore a materially distinct, targeted falsifier.

To prevent tuning, only the chronologically earlier **top-2** E113 protocol is admissible. Top-4 is sealed as an ignored collision and may not be run after seeing top-2.

### Mandatory pre-arm cost receipt
Before creating any workflow arm, commit a numeric cost receipt on the top-2 branch that instantiates its own frozen production formula at:

- `n=1024`
- `N=4096`
- last four layers only
- inherited accounting-complete base `149,114,620,592` FLOPs.

The receipt must separately bill:
- top-2 scans;
- group-code construction;
- counts/sums/sumsq for all four states and both independent blocks;
- every Gaussian pdf/cdf and conditional-moment transform under flopscope;
- reductions/finalization.

Apply the protocol's 2x safety factor to the **complete measured incremental envelope**.

Admission requires:
`all_in_upper <= floor(0.13 * 2^41)`.

A missing line item or use of “already computed/free” without evidence is a failed admission gate.

### Sole exact scientific run
If and only if the cost receipt passes, execute exactly one small-width exact falsifier from the frozen top-2 protocol:

- latent input dimension: 2
- width: 8
- depth: 4
- zero bias
- PCG64 weight seed: 113113
- float64
- recursive exact angular-sector truth
- analytic Rayleigh moments
- no Monte Carlo
- no public/public-mini/scorer/holdout/full data.

Scientific PASS requires all preregistered gates, especially:

- finite exact/candidate values;
- deterministic replay max abs = 0;
- first-layer bias MSE <= `1.89e-8`;
- **every** later-layer mean-bias MSE <= `1.89e-8`;
- final-layer mean-bias MSE <= `1.89e-8`;
- admitted all-in utilization <= `0.13`.

Any failure => terminal E113 NO-GO. No top-4 rescue, parent-count change, seed change, threshold change, rerun, or public diagnostic.

A PASS is **small-exact scientific GO only**. It may authorize one separately preregistered target-free production-shape synthetic gate; it does not authorize public/competition GO.

## Freeze / sequencing

Until the E113 top-2 gate above is resolved:

- do not run E113 top-4;
- do not run E112 Edgeworth as a rescue;
- do not call E110 Householder a breakthrough;
- do not launch another public/mini/scorer/holdout/full diagnostic;
- do not merge same-ID metrics;
- do not reuse E104 mini0 errors to tune any successor.

Audit decision:
`NO_CURRENT_COMPETITION_BREAKTHROUGH_GO__NEXT_GATE_E113_TOP2_EXACT`.


## Historical GO-surface cleanup

A final branch-name sweep covered older branches returned by the repository around the historical GO surface:

- `research/e031-output-hessian-diagonal-20260915` currently points exactly to canonical bootstrap head `29bee3f8d23fc620b77aaed414b1b7a928af4b83` and contains no E031-specific commit history. It has **zero experiment evidence** and must not be cited as an E031 result or GO.
- `research/e033-diagonal-edgeworth-20260915@c97ac30f76ef4cda8e9754d644ed4003f7667842` is explicitly terminal diagonal Edgeworth NO-GO.
- `research/e036-output-hessian-diagonal-20260915@97c9e88b88b20029fb7b61652ab437910bbbc635` is explicitly terminal centered-Hessian NO-GO.

These historical branches add no surviving breakthrough GO.
